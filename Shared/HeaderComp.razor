<br />
<br />
<header class="container text-left pt-5 my-3">
    @* date/author *@
    <div class="row">
        <div>
            <span>
                <small>
                    @* TODO: make author link dynamic later *@
                    <a href="about" title="author">
                        <i>
                            VVGonline
                        </i>
                    </a>
                </small>
            </span>
            <span>
                &nbsp;|&nbsp;
                <small>
                    <i>
                        @articlePublishDate
                    </i>
                </small>
            </span>
        </div>
    </div>
    @* title and subheading *@
    <div class="row">
        <article class="col">
            @* title *@
            <h2 class="display-2">
                @articleTitle
            </h2>
            @* keyline *@
            <div class="h4 pb-4">
                <b>
                    "@keyLine"
                </b>
            </div>
        </article>
    </div>
    @* banner *@
    <div class="row pb-3">
        <img src="@bannerImagePath" alt="@articleTitle" class="img-fluid rounded" loading="lazy" />
    </div>
</header>
@code {
    [Parameter]
    public string? articleTitle { get; set; }
    [Parameter]
    public string? keyLine { get; set; }
    [Parameter]
    public string? articlePublishDate { get; set; }
    [Parameter]
    public string? bannerImagePath { get; set; }
    string getArticleTitile() => articleTitle is null ? "VVGonline" : $"{articleTitle}";

    string GetSubTitle()
    {
        if (string.IsNullOrEmpty(keyLine))
            return "";
        return keyLine.TrimEnd('.') + ".";
    }
}